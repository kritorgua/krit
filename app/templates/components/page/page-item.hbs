<section class="mb-3">

  {{#if showTitle}}
    <div class="container">
      <div class="row">
        <div class="col-12 col-sm-12 col-md-8">
          <h1>{{item.title}}</h1>
        </div>
      </div>
    </div>
  {{/if}}

  <div class="container">
    <div class="row">
      <div class="col-12 col-sm-12 col-md-8">
        {{{item.description}}}
      </div>
    </div>
  </div>

</section>
